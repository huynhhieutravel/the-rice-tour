---
// Component ShareButtons
const currentUrl = Astro.url.href;
const encodedUrl = encodeURIComponent(currentUrl);

const fbShareUrl = `https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`;
const zaloShareUrl = `https://zalo.me/share?url=${encodedUrl}`;
---

<div class="bg-surface rounded-2xl p-5 lg:p-6 shadow-sm border border-border">
  <div class="font-bold text-gray-900 uppercase tracking-wider text-sm mb-4 border-b border-border pb-3">Chia sẻ bài viết</div>
  <div class="flex gap-3">
    <!-- Facebook -->
    <a href={fbShareUrl} target="_blank" rel="noopener noreferrer" aria-label="Share on Facebook" class="w-10 h-10 rounded-full bg-[#1877f2] text-white flex items-center justify-center hover:bg-blue-700 transition-colors">
      <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
    </a>
    
    <!-- Zalo -->
    <a href={zaloShareUrl} target="_blank" rel="noopener noreferrer" aria-label="Share on Zalo" class="w-10 h-10 rounded-full bg-[#0068ff] text-white flex items-center justify-center hover:bg-blue-700 transition-colors font-bold text-xs">
      Zalo
    </a>
    
    <!-- Copy Link -->
    <button type="button" aria-label="Copy link" class="btn-copy-link relative w-10 h-10 rounded-full bg-subtle text-secondary flex items-center justify-center hover:bg-brand-600 hover:text-white transition-colors group">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path></svg>
      <span class="copy-tooltip absolute -top-8 left-1/2 -translate-x-1/2 bg-gray-800 text-white text-[10px] px-2 py-1 rounded opacity-0 transition-opacity whitespace-nowrap pointer-events-none">Copy link</span>
    </button>
  </div>
</div>

<script>
  document.querySelectorAll('.btn-copy-link').forEach(btn => {
    btn.addEventListener('click', async (e) => {
      e.preventDefault();
      try {
        await navigator.clipboard.writeText(window.location.href);
        const tooltip = btn.querySelector('.copy-tooltip');
        if (tooltip) {
          tooltip.textContent = 'Đã copy!';
          tooltip.classList.remove('opacity-0');
          setTimeout(() => {
            tooltip.classList.add('opacity-0');
            setTimeout(() => tooltip.textContent = 'Copy link', 300);
          }, 2000);
        }
      } catch (err) {
        console.error('Failed to copy', err);
      }
    });
  });
</script>
